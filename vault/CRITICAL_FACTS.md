# Critical facts — always loaded (~120 tokens)

Keep ≤150 tokens. Review monthly; update when facts change materially.

**User:** Full-stack + PM; Thai primary, English for code/jargon; prefers lean over exhaustive (pick minimum viable scope).

**Repo:** `h:/POC-2smooth/ai-workflow-obsidian` — Obsidian vault + Claude Code skills POC for a "document partner" workflow (SOW → SRS/SDS/WBS/etc.) + second-brain knowledge layer. Git branch `master` tracks `origin/master`.

**Active projects:** POC itself; no real client project yet. `projects/sample-loyalty/` is a synthetic fixture.

**Hard preferences (override defaults):**
- Lean over exhaustive — one generic skill + config beats many specialized ones
- Ask before doing high-blast-radius actions (destructive git, env changes) even if user seems authorized
- Every knowledge note has `confidence` + `last-accessed` + `review-after`; opinions flagged `> [!opinion]`; provenance kept in `_raw/`
- Doc-workflow and knowledge-hub are separate concerns — don't conflate `update-doc` (delivery) with `update-knowledge` (second brain)

**Last reviewed:** 2026-04-13
